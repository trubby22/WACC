begin
  void f ( bool b ) is
    if ( b ) then
      return
    else
      skip
    fi
  end
  void g ( bool b ) is
    if ( b ) then
      skip
    else
      return
    fi
  end
  call f ( false ) ;
  call g ( true )
end